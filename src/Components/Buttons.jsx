import React from 'react'

const Buttons = ({name}) => {
  return (
        <button className='m-2 bg-gray-200 rounded-lg px-3 py-1'>
            {name}
        </button>

  )
}

export default Buttons